node_modules/
.probes/
out/
*.log
.DS_Store

# Hotkey app: the .app bundle is a build product tree (the repo tracks the small
# cmd/* binaries, but a bundle is noise) plus the dev-only measuring instrument.
app/build/
app/tools/inkbox/inkbox

# Verification screenshots taken while building surfaces.
.screenshots/

# Test recordings staged into render/public for the v1 Remotion-based renderer
# (backlogged). Regenerated per run by the cli.
render/public/source.*

# Stray cross-project agent memory that landed here at some point — belongs
# in the workspace-level ~/Code/.claude/, not this repo.
.claude/agent-memory/linear-workflow/

# Packaged install artifacts (built by app/package.sh, published to Releases)
app/dist/
